#include<stdio.h>
int main(){
    // write a program to print "Hello world!"
    printf("Hello world!");
    return 0;
    // create a program to find the sum of two numbers entered by the user 
    int myNum = 100+25;
    printf("%d,myNum");
    return 0;
    // impliment a program to check whether a number entered by the user is even or odd
    int main(){
        int num;
        printf("Enter an integer : ");
        scanf("&d",&num);

        // true if number is divisible by 2
        if (num % 2 == 0)
        printf("%d is even.",num);
        else
        printf("%d is odd.",num);

        return 0;


    };

    //Creat a program to check whether a given number is prime or not
    
}